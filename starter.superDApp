type = //"JS" or "iframe"
title = "App Title"
code = //enter app code as a "string" if type is "JS". if you need to do appendChild to app use the App object
url = //enter app url as a "string" if type is "iframe"
